%table{class: "table table-striped table-condensed", id: "bets"}
  %thead
    %tr
      %th Player To Win
      %th Points
      %th Odds
      %th Challenger
      %th
  %tbody
    - match = @stream.betable_match
    - match.bets.each do |bet|
      %tr
        %td= match.send(:"player#{bet.player}")
        %td= bet.points
        %td= bet.odds
        %td= link_to bet.challenger, "/users/#{bet.challenger}"
        %td= link_to "Accept", "#"
